# IECS 網站 × EduAI 整合說明

把 IECS 前端網站接上本地 EduAI AI 後端，原本的假畫面現在是真的 AI 生成。

---

## 架構

```
IECS 前端 (3000)  ──┬──►  IECS 後端 Node (3306)   使用者 / 登入 / 論壇
                    └──►  EduAI 後端 Python (8000) 簡報 / 測驗 / 問答
```

前端透過 `eduai.js` 這層橋接呼叫 EduAI，兩個後端各自獨立、互不影響。

---

## 啟動步驟（三個終端機）

**1. AI 後端（EduAI）** — 在 `eduai/api/` 資料夾：
```bash
python -m uvicorn app:app --host 127.0.0.1 --port 8000
```
> 需要先安裝 Ollama 並建立 `eduai-qwen` / `eduai-fast` 模型，詳見 EduAI 的安裝說明。

**2. 網站後端（使用者系統）** — 在本資料夾：
```bash
npm install
npx prisma generate
npm run dev
```

**3. 前端網頁** — 在本資料夾：
```bash
python dev-server.py 3000
```

瀏覽器開啟 **http://localhost:3000**

---

## 已接上 AI 的頁面

| 頁面 | 功能 | 對應 EduAI 端點 |
|---|---|---|
| `presentation.html` | 主題／YouTube／PDF → 互動簡報 | `/make_html_slide`、`/youtube_slide` |
| `quiz.html` | 主題／教材 → 互動測驗（可設題數） | `/make_quiz_html` |
| `text-page.html` | 主題／PDF → 教學講義內容 | `/chat`、`/upload_pdf` |

`video-page.html`（影音合成）**尚未接上**，仍是靜態畫面。

---

## 簡報頁功能

- **三種輸入**：課程主題、YouTube 影片連結（自動抓字幕）、上傳 PDF 教材
- **五種視覺風格**：瑞士國際、深藍科技、簡約米白、暗紫創意、森林學術
- **生成進度即時顯示**（本地模型需數分鐘，會顯示目前階段與經過時間）
- **側欄大綱自動填入**真實章節標題
- 簡報播放：`←→` 翻頁、`F` 全螢幕、`T` 縮圖、`P` 簡報者模式（含講者備註）、`Ctrl+P` 匯出 PDF

---

## 常見問題

**畫面顯示「無法連線到 EduAI」**
AI 後端沒啟動。照上面步驟 1 啟動 port 8000 的服務。

**生成很久沒反應**
正常。本地 14B 模型生成一份簡報約需數分鐘，狀態列會顯示進度與已經過時間。

**登入／註冊失敗**
網站後端（Node）沒啟動，或 `.env` 的 `PORT` 與 `common.js` 的 `API_BASE_URL` 不一致。
目前兩邊都是 **3306**。

**想改 AI 後端位址**
編輯 `eduai.js` 最上方的 `EDUAI_BASE_URL`。
